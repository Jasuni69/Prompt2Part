# Core dependencies
requests>=2.28.0
python-dotenv>=1.0.0

# For RAG (vector search, etc.)
chromadb>=0.4.0
openai>=1.0.0
tiktoken>=0.5.0

# For LLM integration
ollama>=0.1.0  # Optional, for local LLM using Ollama

# For CLI and optional GUI
click>=8.1.0
tqdm>=4.65.0
PyQt5>=5.15.0  # Optional, for GUI 