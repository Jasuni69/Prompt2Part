module rock(width = 1) {
	n = 15 * rands(1, 1.25, 1)[0];
	r = width / 2 * rands(1, 1.25, 1)[0];
    theta = rands(0, 359, n);
	phi = rands(0, 359, n);
	
	scale([1, 1.25, 2])
	polyhedron_hull([
	    for(i = [0:n - 1]) [r * cos(theta[i]), r * sin(theta[i]), r * cos(phi[i])]]);
}