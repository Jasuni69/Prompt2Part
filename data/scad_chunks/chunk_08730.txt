function spherical_coordinate(point) = 
    // mathematics [r, theta, phi]
    point == [0, 0, 0] ? point :
    let(r = norm(point))
    [
        r, 
        atan2(point.y, point.x), 
        acos(point.z / r)
    ];