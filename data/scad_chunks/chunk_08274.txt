function shape_square(size, corner_r = 0) = 
    let(
        is_flt = is_num(size),
        x = is_flt ? size : size.x,
        y = is_flt ? size : size.y        
    )
    __trapezium(
        length = x, 
        h = y, 
        round_r = corner_r
    );