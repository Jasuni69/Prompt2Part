function _inner(stroke_pts, path_pts, leng_path_pts, scale_step) =
    [
        for(i = 1; i < leng_path_pts; i = i + 1)
        _stroke(
            stroke_pts, 
            path_pts[i - 1], 
            path_pts[i ], 
            scale_step, 
            i 
        )
    ];