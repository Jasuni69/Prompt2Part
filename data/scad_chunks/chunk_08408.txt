function _neighbors(fcord, seed, grid_w, gwv) = 
    let(range = [-1:1])
    [
        for(z = range, y = range, x = range)
        let(
            cord = [x, y, z] + fcord,
            sds = rands(0, 1, 3, cord * gwv + seed)
        )
        (cord + sds) * grid_w
    ];