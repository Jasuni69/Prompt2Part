// Show the pipe connector.
difference()
{
  rotate_extrude(convexity=4)
    translate([3,0])
      polygon(path);

  // Remove holes.
  for(i=[0:4])
  {
    rotate(i*360/5)
      linear_extrude(5,convexity=3,center=true)
        translate([6,0])
          circle(0.5);
  }
}