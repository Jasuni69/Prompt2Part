function __inner_points_shape_star(r1, r2, n) = 
    let(a_step = 360 / n)
    [
        for(i = 0; i < n; i = i + 1) 
        let(a = 90 + a_step * (i + 0.5))
        [r2 * cos(a), r2 * sin(a)]
    ];