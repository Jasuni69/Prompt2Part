function visitCCW(maze, next, currentCell) = 
	update_maze(
		update_maze(
			maze, 
			set_wallType(
				currentCell, 
				get_wallType(currentCell) == INWARD_CCW_WALL ? INWARD_WALL : NO_WALL
			)
		), 
		set_visited(next)
	);