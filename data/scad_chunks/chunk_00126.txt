module zipties()
    layout([for(z = zipties) 9], 2 * wire_r) {
        ziptie(zipties[$i], wire_r, max(t, 0));

        if(t >= 0)
            color(grey(20))
                cylinder(r = wire_r, h = 10, center = true);

    }

if($preview)
    zipties();