module addHullStep(onlyFor=currentPart(),addOnly=true,removeOnly=false)
{
  if((removeOnly ||!(addOnly && $removing)) && 
  	  !(removeOnly && $removing) && 
  		partIs(onlyFor)) hull()
    {
       $hulling=true;
       children();
    }
  children();
}